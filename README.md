# Yoyo — Language App

Monorepo for the Yoyo language learning project.

## Nobi (Mobile)

The **Nobi** Expo React Native app lives in [`mobile/`](mobile/). See [mobile/README.md](mobile/README.md) for setup and environment variables.

```bash
cd mobile
npm install
cp .env.example .env.local
npm start
```
