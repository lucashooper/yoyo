/** Flat UI borders — Apple / Linear style */
export const flatBorder = {
  borderWidth: 1,
  borderColor: 'rgba(0, 0, 0, 0.06)',
} as const;

export const flatBorderStrong = {
  borderWidth: 1,
  borderColor: 'rgba(0, 0, 0, 0.1)',
} as const;
